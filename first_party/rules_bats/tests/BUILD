load("//first_party/rules_bats:defs.bzl", "bats_test")

# This package exists to verify that the bats infrastructure itself works correctly.
# It contains a minimal test case to ensure the macro and runner can execute a simple assertion.
bats_test(
    name = "simple_test",
    srcs = ["simple_test.bats"],
)
