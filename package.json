{
  "name": "monorepo",
  "version": "0.0.0",
  "_comment": "The concept of dev dependencies is meaningless in this repo. A dep can be referenced in multiple places, and the concept of prod/dev is not well defined. Only production deps are used to avoid creating the illusion of a strong separation between prod and dev where none exists.",
  "dependencies": {
    "@eslint/js": "^9.16.0",
    "@types/node": "^18",
    "eslint": "^9.16.0",
    "prettier": "^2.8.7",
    "prettier-plugin-gherkin": "^1.1.0",
    "prettier-plugin-sql": "^0.14.0",
    "@prettier/plugin-xml": "^0.10.0",
    "stylelint": "^16",
    "stylelint-config-standard": "^36.0.1",
    "typescript": "4.9.5",
    "typescript-eslint": "^8.17.0"
  }
}
