load("@bazel_skylib//rules:build_test.bzl", "build_test")

build_test(
    name = "resolved",
    targets = ["@rules_diff//diff/toolchain/diff:resolved"],
)

build_test(
    name = "entrypoint",
    targets = ["@diff//:entrypoint"],
)
