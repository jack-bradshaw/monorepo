# Generated by protoc/private/protoc_toolchains.bzl
#
# These can be registered in the workspace file or passed to --extra_toolchains flag.
# By default all these toolchains are registered by the rules_proto_toolchains macro
# so you don't normally need to interact with these targets.


toolchain(
    name = "linux_aarch_64_toolchain",
    exec_compatible_with = ["@platforms//os:linux", "@platforms//cpu:aarch64"],
    # Bazel does not follow this attribute during analysis, so the referenced repo
    # will only be fetched if this toolchain is selected.
    toolchain = "@toolchains_protoc_hub.linux_aarch_64//:prebuilt_protoc_toolchain",
    toolchain_type = "@rules_proto//proto:toolchain_type",
)

toolchain(
    name = "linux_ppcle_64_toolchain",
    exec_compatible_with = ["@platforms//os:linux", "@platforms//cpu:ppc64le"],
    # Bazel does not follow this attribute during analysis, so the referenced repo
    # will only be fetched if this toolchain is selected.
    toolchain = "@toolchains_protoc_hub.linux_ppcle_64//:prebuilt_protoc_toolchain",
    toolchain_type = "@rules_proto//proto:toolchain_type",
)

toolchain(
    name = "linux_s390_64_toolchain",
    exec_compatible_with = ["@platforms//os:linux", "@platforms//cpu:s390x"],
    # Bazel does not follow this attribute during analysis, so the referenced repo
    # will only be fetched if this toolchain is selected.
    toolchain = "@toolchains_protoc_hub.linux_s390_64//:prebuilt_protoc_toolchain",
    toolchain_type = "@rules_proto//proto:toolchain_type",
)

toolchain(
    name = "linux_x86_64_toolchain",
    exec_compatible_with = ["@platforms//os:linux", "@platforms//cpu:x86_64"],
    # Bazel does not follow this attribute during analysis, so the referenced repo
    # will only be fetched if this toolchain is selected.
    toolchain = "@toolchains_protoc_hub.linux_x86_64//:prebuilt_protoc_toolchain",
    toolchain_type = "@rules_proto//proto:toolchain_type",
)

toolchain(
    name = "osx_aarch_64_toolchain",
    exec_compatible_with = ["@platforms//os:macos", "@platforms//cpu:aarch64"],
    # Bazel does not follow this attribute during analysis, so the referenced repo
    # will only be fetched if this toolchain is selected.
    toolchain = "@toolchains_protoc_hub.osx_aarch_64//:prebuilt_protoc_toolchain",
    toolchain_type = "@rules_proto//proto:toolchain_type",
)

toolchain(
    name = "osx_x86_64_toolchain",
    exec_compatible_with = ["@platforms//os:macos", "@platforms//cpu:x86_64"],
    # Bazel does not follow this attribute during analysis, so the referenced repo
    # will only be fetched if this toolchain is selected.
    toolchain = "@toolchains_protoc_hub.osx_x86_64//:prebuilt_protoc_toolchain",
    toolchain_type = "@rules_proto//proto:toolchain_type",
)

toolchain(
    name = "win64_toolchain",
    exec_compatible_with = ["@platforms//os:windows", "@platforms//cpu:x86_64"],
    # Bazel does not follow this attribute during analysis, so the referenced repo
    # will only be fetched if this toolchain is selected.
    toolchain = "@toolchains_protoc_hub.win64//:prebuilt_protoc_toolchain",
    toolchain_type = "@rules_proto//proto:toolchain_type",
)
