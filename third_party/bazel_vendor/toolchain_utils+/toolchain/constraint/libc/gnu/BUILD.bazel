load(":versions.bzl", "VERSIONS")

constraint_setting(
    name = "gnu",
    visibility = ["//visibility:public"],
)

[
    constraint_value(
        name = version,
        constraint_setting = ":gnu",
        visibility = ["//visibility:public"],
    )
    for version in VERSIONS
]
